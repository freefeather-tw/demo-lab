server:
    port: 8282

spring:
  application:
    name: demo2
  sleuth:
    sampler:
      probability: 1.0
  #zipkin:
  #  base-url: http://localhost:9411

logging:
  level:
    root: ERROR
    org.springframework: ERROR
    com.example: DEBUG

cors:
  allowed:
    origin: "http://localhost:8080"